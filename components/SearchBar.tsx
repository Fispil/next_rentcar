import React from 'react'

const SearchBar = () => {
  const handleFormSubmit = () => {
    
  }

  return (
    <form className='searchbar' onSubmit={handleFormSubmit}></form>
  )
}

export default SearchBar